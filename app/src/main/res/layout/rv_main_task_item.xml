<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/task_item_background"
    android:padding="@dimen/default_padding"
    android:id="@+id/rv_task_item"
    >
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_weight="1"
        >
        <TextView
            android:id="@+id/tv_rv_main_task_date"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="0dp"
          />

        <TextView
            android:id="@+id/tv_rv_main_task_title"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="0dp"
      />
    </LinearLayout>

    <ImageButton
        android:visibility="gone"
        android:id="@+id/btn_sync_task"
        android:text="@string/btn_sync_task_text"
        android:background="@drawable/ic_choose_sync_calendar_on"
        style="@style/rv_button"
    />

    //TODO change back

    <ImageButton
        android:id="@+id/btn_delete_task"
        style="@style/rv_button"
        android:background="@drawable/ic_delete_task"
        />
    <!--<ImageButton-->
        <!--android:id="@+id/btn_detailed_task"-->
        <!--style="@style/rv_button"-->
        <!--android:background="@drawable/ic_detailed_task"-->
        <!--android:text="@string/btn_sync_task_text"/>-->
    <!--<ImageButton-->
        <!--android:id="@+id/btn_delete_task"-->
        <!--android:text="@string/btn_sync_task_text"-->
        <!--android:background="@drawable/ic_delete_task"-->
        <!--style="@style/rv_button"-->
        <!--/>-->
</LinearLayout>